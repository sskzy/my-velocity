package ${PACKAGE_NAME}.${MODULE_NAME}.controller.manager;

import io.swagger.annotations.Api;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;
import springfox.documentation.annotations.ApiIgnore;

/**
 * @author : ${AUTHOR_NAME}
 * @since : ${DATE} ${TIME}
 */
@ApiIgnore
@RestController
@RequiresBackendLogin
@RequestMapping("/manager/${class_name}")
@Api(value = "Manager${CLASS_NAME}Controller", tags = "")
public class Manager${CLASS_NAME}Controller extends AbsController<${CLASS_NAME}>{

}
